export default function TaskDetail() {
  return (
    <section>
      <p className="text-sm font-semibold uppercase tracking-[0.18em] text-signal">Live trace</p>
      <h2 className="mt-2 text-3xl font-semibold">Task detail</h2>
      <p className="mt-2 max-w-2xl text-sm leading-6 text-steel">
        Step 15 will add the live LangGraph visualization, WebSocket event stream, output panel,
        and validation errors.
      </p>
    </section>
  )
}
