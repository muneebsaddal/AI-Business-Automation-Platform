[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_SHOWCASE_MODE = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
